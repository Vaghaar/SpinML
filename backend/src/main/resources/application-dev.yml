# Profil développement — surcharges de application.yml
app:
  jwt:
    cookie-secure: false   # HTTP localhost — pas de flag Secure sur le cookie

spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/spinmylunch
    username: spinmylunch
    password: "Ry@nD.orken005"
  jpa:
    properties:
      hibernate:
        format_sql: true
  data:
    redis:
      port: 6380
      password: "Ry@nD.orken005"
logging:
  level:
    com.spinmylunch: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql: TRACE

management:
  endpoint:
    health:
      show-details: always
