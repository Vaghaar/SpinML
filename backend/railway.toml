[build]
builder = "NIXPACKS"

[deploy]
startCommand = "java -jar target/spinmylunch-backend-0.1.0-SNAPSHOT.jar"
healthcheckPath = "/actuator/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
